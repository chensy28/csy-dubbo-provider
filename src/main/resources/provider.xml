<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 2.6.1之前版本的域名是code.alibabatech.com ，之后的域名是dubbo.apache.org -->

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="hello-world-app"  />

    <!-- 使用multicast广播注册中心暴露服务地址 -->
    <!--<dubbo:registry address="multicast://224.5.6.7:1234" />-->

    <dubbo:registry address="zookeeper://127.0.0.1:2181?client=zkclient" />

    <!-- 用dubbo协议在20880端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="20880" />

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.csy.dubbo.provider.DemoService" ref="demoService" />
    <dubbo:service interface="com.csy.dubbo.provider.DemoServiceV2" ref="demoServiceV2"/>

    <!-- 和本地bean一样实现服务 -->
    <bean id="demoService" class="com.csy.dubbo.provider.DemoServiceImpl" />
    <bean id="demoServiceV2" class="com.csy.dubbo.provider.DemoServiceImplV2" />
</beans>